public class profilecontact {
    
    public static void  profilecontact (list<Account>acclist){
        Set<Id> Account_Id_Set = new Set<Id>();
        list<contact> conlist = new list<contact>();
        for(Account a : acclist){
            if(a.website != null){
                Account_Id_Set.add(a.id);
            }
        }
        if(AccountId_Set.size()>0){
            List<Contact> c = [select Id,Firstname,Lastname,Profile__c,Accountid,Account.website from contact where Accountid in :i
            for(Contact contact_list : c){
                contact_list.Profile__c= contact_list.account.website + '/' + contact_list.firstname.left(1) + contact_list.lastname;
                update contact_List;
            }

}
                               